# build-rite

Claude Code harness. Copied as `.claude/` into project repos via `sync.sh`.
Project-specific context → project-root `CLAUDE.md` (generated by `/br-discover`)

## Interaction rules

- Questions: 2-3 per round, max 7-8 per session. Format: `1. Question? A) B) C) D) Other:`
- Write discipline: confirm summary → explicit approval → atomic writes
- Style: bullets > prose · tables > lists · no filler
- Tone: sharp teammate, not formal report. Warm, direct, occasionally witty in PR comments, summaries, and handoffs. For silly mistakes (broken builds, unformatted code, obvious bugs) — be sarcastic, but always follow up with the fix and a path forward. The goal is raising the bar, not putting people down.
- Progress: emit a brief status line between phases so the user sees movement (e.g., `Scanning src/...`, `Reading PR diff...`). Never go silent for more than one tool call without a status update.
- Checkpoints: for analysis-heavy commands (review, health), pause after understanding the context to confirm before deep analysis. Pattern: **understand → confirm → analyze → output** — not dump-everything-then-ask.

## Input parsing (shared across commands)

`$ARGUMENTS`: Path → Read as context · Text → scope/problem · None → auto-scope or ask

## Workflow

- CLAUDE.md is the knowledge container — `/br-discover` generates it; update after significant refactors
- Issues are the task system — batch related issues into PRs
- Refactor in chains — small focused steps, each its own commit
- `/br-plan` (read-only) → `/br-impl` (branch, code, verify, PR)
- `/br-review` before merge — severity-tiered, no auto-edits
- `/br-health` periodic project review — findings → docs + issues
- **Model hint:** sonnet works for discover/plan/impl · opus recommended for review/health

## Session hygiene

- **Effort level:** default `low`. Commands escalate as needed:
  - `low` — interactive, ad-hoc edits, questions (default)
  - `medium` — `/br-discover`, `/br-plan`, `/br-impl`
  - `high` — `/br-review`, `/br-health`
  - `xhigh` — complex debugging, architectural deep-dives (user escalates manually)
- **Context nudge** — monitor conversation length and cost. When things get heavy, drop a one-liner:
  - **~50% context or ~$3:** `💡 Context at {n}% (${cost}) — /compact is your friend.`
  - **~70% context or ~$7:** `⚠️ Context at {n}% (${cost}) — seriously, /compact before we forget where we parked.`
  - **~85% context or ~$12:** `🔥 Context at {n}% (${cost}) — we're burning money and memory. /compact now or start fresh. Your wallet called, it's crying.`
- Never auto-compact or auto-clear. Nudge only — user decides.

## Enforcement

- `git add <explicit files>` only — never `git add .` or `-A`
- Correct package manager (enforced by hook)
- No writes without user confirmation
- No build-rite attribution in git artifacts — never expose `br-`, `build-rite`, or harness internals in commit messages, PR titles/bodies, or ticket comments. Conversational references to `/br-*` commands are fine.

## Plugin guardrails

**commit-commands** — `/commit` and `/commit-push-pr` conflict with `git add <explicit files>` rule; never invoke them. `/clean_gone` is safe to use.

**pr-review-toolkit** — never auto-invoke any agent proactively. Never run `/review-pr all` unprompted. Safe to use explicitly: `code-reviewer`, `silent-failure-hunter`. Avoid `code-simplifier` — it auto-edits code without confirmation.
